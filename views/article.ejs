<%- include("partials/header.ejs") %>
<%- include("partials/navbar.ejs") %>

<body>
    <div class="card mb-4">
        <hr>
        <div class="card-header">
            <h2><%= article.title %></h2>
        </div>
        <div class="card-body">
            <%- article.body %>
        </div>
        <div class="card-footer text-muted">
            <% 
                    var date = article.createdAt.getUTCDate();
                    var month = article.createdAt.getUTCMonth() + 1;
                    var year = article.createdAt.getUTCFullYear();
                    var dateStr = date + "/" + month + "/" + year;
            %> 
            <p>Postado em <%= dateStr %> [ Autor:<%= ownedBlog %> ]</p>
        </div>
        </div>
</body>

<%- include("partials/footer.ejs") %>   
